import type { Metadata } from "next";
import { Montez, Geist, Geist_Mono, Exo_2, Audiowide } from "next/font/google";
import "./globals.css";
import ReduxProvider from "./ReduxProvider";
import ThemeProvider from "@/components/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

const montez = Montez({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-montez",
  display: "swap",
});

const exo2 = Exo_2({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-exo2",
  display: "swap",
});

const audioWide = Audiowide({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-audioWide",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Igotmessage App",
  description: "Generated by create next app",
  icons: {
    icon: [
      {
        url: "/logos/icon.png",
        type: "image/png",
        media: "(prefers-color-scheme: dark)",
      },
    ],
    shortcut: "/logos/icon.png",
  },
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#000000" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${montez.variable} ${exo2.variable} ${audioWide.variable}`}
    >
      <body className="flex bg-black items-center justify-center h-full w-full">
        <ReduxProvider>
          <ThemeProvider>{children}</ThemeProvider>
        </ReduxProvider>
      </body>
    </html>
  );
}
